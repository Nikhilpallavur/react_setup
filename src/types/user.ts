export type userDataItem = {
  key?: string;
  userCode: string;
  userName: string;
  reporterId: string;
  roleId?: {
    role: string;
  };
  contactNumber?: string;
  email?: string;
  _id: string;
};
